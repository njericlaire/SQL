DECLARE @X int --declare a variable

SET @X=100--SET THE VALUE OF VARIABLE

SELECT @X

--------DECLARE 2 VARIABLE TOGETHER-------
DECLARE @X int
DECLARE @Y VARCHAR(20)

SET @X=100
SET @Y='HELLO'

SELECT @X
SELECT @Y

--------DECLARE VARIABLES IN ONE LINE-----
DECLARE @X INT, @Y VARCHAR(10)

SET @X=100
SET @Y='HELLO'

SELECT @X,@Y


----------------------
DECLARE @X INT

SET @X=100

SET @X=700

SELECT @X

-----ASSIGNING DEFAULT VALUES---------
DECLARE @X INT=100

SET @X=700

SELECT @X




